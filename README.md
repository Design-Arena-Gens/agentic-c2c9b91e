# Telegram Python Bot on Vercel

بوت تيليغرام مكتوب بلغة بايثون يعمل عبر دوال Serverless على منصة Vercel مع واجهة ويب توضح خطوات التشغيل.

## 👣 المتطلبات

- Python 3.10 أو أحدث
- حساب تيليغرام والحصول على توكن البوت من خلال @BotFather
- حساب Vercel مفعّل

## 📂 هيكل المشروع

```
.
├── api/
│   └── telegram_bot.py   # دالة السيرفر التي تستقبل رسائل الويب هوك
├── index.html            # صفحة ويب باللغة العربية تشرح الاستخدام
├── requirements.txt      # الحزم اللازمة للدالة
└── tools/
    └── set_webhook.py    # أداة لتعيين الوِب هوك للبوت
```

## ⚙️ التشغيل محلياً

1. ثبّت الحزم:
   ```bash
   pip install -r requirements.txt
   ```
2. عرّف متغير البيئة `TELEGRAM_BOT_TOKEN`.
3. استخدم ngrok (أو بديل مشابه) لإنشاء رابط HTTPS عام.
4. ثبت الوِب هوك:
   ```bash
   python tools/set_webhook.py https://YOUR-NGROK-ID.ngrok.app/api/telegram_bot
   ```
5. أرسل رسائل للبوت وتحقق من الردود على الأوامر `/start` و`/help` و`/time` و`/dice`.

## ☁️ النشر على Vercel

1. أضف متغير البيئة `TELEGRAM_BOT_TOKEN` من لوحة تحكم Vercel.
2. انشر المشروع:
   ```bash
   vercel deploy --prod --yes --name agentic-c2c9b91e
   ```
3. حدّث الوِب هوك ليشير إلى `https://YOUR-DOMAIN/api/telegram_bot` باستخدام الأداة في مجلد `tools`.

## 🧠 منطق الرد

الدالة في `api/telegram_bot.py` تستقبل الرسائل النصية وتدعم:

- `/start` للترحيب ووصف البوت
- `/help` لعرض قائمة الأوامر
- `/time` لإرجاع الوقت الحالي (UTC)
- `/dice` لمحاكاة رمي نرد
- أي رسالة أخرى يتم تكرارها مع تأكيد الاستقبال

يمكنك تعديل الدالة `_handle_message` لإضافة أوامر جديدة بسهولة.

## 🧪 الاختبارات

للتحقق من خلو المشروع من أخطاء تركيبية:
```bash
python -m compileall api tools
```

## 📬 إعدادات إضافية

- لتغيير لغة الواجهة أو طريقة العرض قم بتعديل `index.html`.
- أضف تبعيات بايثون جديدة عبر تحديث `requirements.txt`.
- جرّب تخزين الردود المتقدمة عبر ربط البوت بقاعدة بيانات أو خدمة API أخرى.

بالتوفيق في بناء بوتاتك! 🚀
